{
  "name": "chatgpt-buddy-extension",
  "version": "1.0.0",
  "description": "Browser extension for ChatGPT-buddy",
  "scripts": {
    "build": "tsc && cp manifest.json build/ && cp -r assets build/",
    "clean": "rimraf build",
    "dev": "npm run build && npm run watch",
    "watch": "tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/ws": "^8.5.10",
    "rimraf": "^5.0.1",
    "typescript": "^5.5.3"
  },
  "manifest": {
    "host_permissions": [
      "<all_urls>"
    ]
  }
}

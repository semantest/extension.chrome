{
  "name": "@semantest/extension",
  "version": "2.0.0",
  "description": "Browser extension for Semantest - AI automation extension built on Semantest framework",
  "scripts": {
    "build": "node build.js",
    "build:old": "tsc && npm run copy-assets",
    "copy-assets": "cp manifest.json build/ && cp -r assets build/ && cp src/*.html build/ 2>/dev/null || true",
    "clean": "rimraf build && mkdir -p build",
    "dev": "npm run build && npm run watch",
    "watch": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && cd build && zip -r ../semantest-extension.zip .",
    "validate": "node -e \"const m=require('./build/manifest.json'); console.log('✅ Extension:', m.name, 'v' + m.version)\""
  },
  "keywords": [
    "semantest",
    "ai-automation", 
    "browser-extension",
    "training-system",
    "event-driven",
    "intelligent-automation"
  ],
  "author": "Semantest Team <team@semantest.com>",
  "license": "GPL-3.0",
  "homepage": "https://semantest.com/packages/extension",
  "repository": {
    "type": "git",
    "url": "https://github.com/semantest/semantest.git"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.5",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "eslint": "^8.56.0",
    "fs-extra": "^11.2.0",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.2",
    "typescript": "^5.5.3"
  }
}